services:
  cloudmail-key-panel:
    build: .
    container_name: cloudmail-key-panel
    restart: unless-stopped
    ports:
      - "127.0.0.1:${APP_PORT:-38080}:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
